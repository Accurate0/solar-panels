name: Deploy
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-web:
    permissions:
      packages: write

    uses: Accurate0/workflows/.github/workflows/build-push-docker.yaml@1daec34b2444f1c75a17f58ed0a70a148c0def3b # main
    with:
      directory: web
      image-name: solar-web
      docker-build-args: ""
      use-rust-caching: true

  build:
    permissions:
      packages: write

    uses: Accurate0/workflows/.github/workflows/build-push-docker.yaml@1daec34b2444f1c75a17f58ed0a70a148c0def3b # main
    with:
      directory: .
      image-name: solar
      docker-build-args: |
        BINARY_NAME=solar
      use-rust-caching: true

  deploy:
    needs:
      - build
      - build-web
    uses: Accurate0/workflows/.github/workflows/deploy-app-k8s-v2.yaml@1daec34b2444f1c75a17f58ed0a70a148c0def3b # main
    with:
      service-name: solar-panels
      images: ghcr.io/accurate0/solar,ghcr.io/accurate0/solar-web
      manifests-path: projects/solar-panels/manifests
      tag: ${{ github.sha }}
    secrets:
      FORGEJO_TOKEN: ${{ secrets.FORGEJO_CI_IMAGE_UPDATER }}
